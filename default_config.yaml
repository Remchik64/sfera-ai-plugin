# Sfera AI Bridge Plugin Configuration
# SSE settings for self-activation
sse_enabled: true
keepalive_interval: 30
pending_events_ttl: 86400  # 24 hours in seconds
max_pending_events: 100
broadcast_on_scheduler_task: true
